package terminal

// what a `start` says while it finishes what a first run never landed
// (../../cmd/better-giving/start.go's finishing).

// RegisteringSpamProtection is what stands over that press, drawn by the caller while it makes it
// (./waiting.go).
//
// **it names the one thing being made rather than the press it belongs to.** the reads in front of
// it report nothing and land in a round trip; what takes the seconds is the widget — the account's
// own list walked, the widget made and both its halves written as vars — and an operator watching a
// terminal say nothing through those is watching a console that has hung.
//
// the words are ./account.go's and ../../cmd/better-giving/start.go's for the same thing: what a
// first deploy says it is about to make is spam protection, and a run that calls it something else
// here is a run naming two things.
func RegisteringSpamProtection() string {
	return "registering spam protection for this deployment"
}
