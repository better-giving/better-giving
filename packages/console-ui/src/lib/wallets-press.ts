// the press that registers the hostnames a donor is drawn wallet buttons on.
//
// a module of its own rather than a literal in either fold, because two folds read this deployment's
// wallet registrations and one press repairs both: the payments fold reports where every hostname
// stands, the sites fold is where a hostname arrives, and the levelling they both want is one call
// on the deployment's own list (../api/client.ts).
//
// **it is the repair and never the ordinary way one gets registered.** the Stripe keys run levels
// them as its last step and the sites press levels them behind the list it stored, so a deployment
// reaches this press already covered — what it is for is a custom domain attached to the worker
// after a setup, which no earlier press can have known about.
//
// no hostname is named here and none may be: the list is the deployment's own address and its own
// site rows, settled inside the worker (`packages/app/src/routes/console.wallet-domains.ts`).

/** what the press that levels the wallet registrations posts, and the whole of what it carries. */
export const WALLETS_INTENT = 'wallets';
